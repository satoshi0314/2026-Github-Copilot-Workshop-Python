# ポモドーロタイマーWebアプリ アーキテクチャ案

## 1. ディレクトリ構成例

```
1.pomodoro/
├── app.py                # Flaskアプリ本体
├── timer/
│   ├── __init__.py
│   ├── logic.py         # タイマー・進捗ロジック
│   └── storage.py       # データアクセス層
├── static/
│   ├── css/
│   │   └── style.css     # カスタムCSS
│   └── js/
│       └── timer.js      # タイマーのロジック（フロントエンド）
├── templates/
│   └── index.html        # メイン画面テンプレート
├── tests/
│   ├── test_logic.py    # ロジックのユニットテスト
│   ├── test_storage.py  # データ層のユニットテスト
│   └── test_app.py      # Flaskアプリ/APIのテスト
└── data/
    └── progress.json     # 進捗データ保存用（必要に応じて）
```

## 2. 機能分担

- **Flask（バックエンド）**
  - ルーティング（画面表示、APIエンドポイント）
  - 進捗データの保存・取得（ファイルやDB、セッションなど）
  - テンプレートレンダリング（Jinja2）

- **HTML/CSS（フロントエンドUI）**
  - UIレイアウト、スタイリング
  - 円形プログレスバーやボタンのデザイン

- **JavaScript（フロントエンドロジック）**
  - タイマーのカウントダウン・進捗アニメーション
  - 「開始」「リセット」ボタンの動作
  - 必要に応じてAPIと通信（fetch/AJAX）

## 3. 主要な画面・API設計

- `/`  
  - メイン画面（index.html）
  - タイマーUI、進捗表示

- `/api/progress`  
  - 今日の進捗取得（GET）
  - 進捗記録（POST）

- 必要に応じて `/api/reset` など

## 4. 状態管理

- タイマー自体はJavaScriptで管理（クライアント側）
- 進捗データはFlask経由で保存・取得（サーバー側）

## 5. ユニットテスト容易化のための工夫

1. **Flaskアプリの構造をモジュール化**
   - `create_app()` ファクトリパターンを採用し、テスト時に設定を切り替えやすくする。
   - ルーティングやロジックをBlueprintで分割。

2. **ビジネスロジックの分離**
   - タイマーや進捗計算などのロジックは、Flaskルートから分離し、独立したPythonモジュール/クラスとして実装。
   - これにより、Webサーバーを立ち上げずにロジック単体でテスト可能。

3. **データアクセス層の抽象化**
   - 進捗データの保存・取得処理をラップするクラスや関数を用意し、テスト時はモックやインメモリ実装に差し替えやすくする。

4. **APIエンドポイントのテスト**
   - Flaskのテストクライアント（`app.test_client()`）を使い、APIのリクエスト/レスポンスを自動テスト。

5. **フロントエンドのテスト**
   - JavaScriptのロジックも関数化し、JestやVitestなどで単体テスト可能な構造にする。

---

このアーキテクチャにより、UIの再現性・拡張性・保守性・テスト容易性が高いWebアプリを実現できます。